# MIDAD-001 — الدفع والتسليم

## الحالة الحالية

يعمل المنتج حالياً بمسار **التحويل البنكي اليدوي** بقيمة 19 درهماً. يسجل الطالب بياناته ومرجع العملية ويرفع إثبات الدفع. تحفظ البيانات في قاعدة البيانات، ويحفظ إثبات الدفع في التخزين الخاص، ثم يبقى الطلب في حالة `pending` إلى أن تتم مراجعته.

لا يعتبر تسجيل الطلب أو رفع الإثبات دليلاً آلياً على نجاح الدفع. لا يتم إصدار رابط الملف إلا بعد اعتماد الطلب من إجراء إداري محمي بالصلاحية `admin`.

## التسليم المحمي

بعد الاعتماد، يستخدم الخادم `storageGetSignedUrl` لإصدار رابط مؤقت مدته 15 دقيقة. يستطيع الطالب طلب الرابط عبر رقم الطلب والبريد الإلكتروني المطابق، ولا يحصل على الرابط إذا كان الطلب غير معتمد أو كان البريد مختلفاً.

## ملف المنتج

ملف MIDAD-001 مرفوع إلى التخزين المرتبط بالمشروع باسم مفتاح داخلي:

`midad-001-law-summary_4382aff1.pdf`

لا ينبغي استخدام مسار `/manus-storage/...` مباشرة في الصفحة العامة، لأن مسار proxy العام لا يمثل طبقة صلاحيات. نقطة التسليم في الراوتر تستخدم رابطاً موقعاً مؤقتاً بدلاً من كشف المفتاح.

## وضع CMI/Simple

تمت إضافة حدّ تكامل واضح في `server/paymentGateway.ts` مع مزودين اثنين: `manual-transfer` و`cmi`. الوضع الحالي هو `manual-transfer` لأن وثائق API أو Merchant ID أو Secret أو callback الرسمي من Simple/CMI لم تُسلَّم للمشروع.

عند الحصول على وثائق التاجر، يجب إضافة القيم التالية في أسرار المشروع، لا في الواجهة أو Git:

- `MIDAD_PAYMENT_PROVIDER=cmi`
- `MIDAD_CMI_MERCHANT_ID`
- `MIDAD_CMI_API_URL`
- `MIDAD_CMI_SECRET`

بعدها يجب بناء adapter خاص بـ CMI والتحقق من توقيع callback قبل تغيير حالة الطلب إلى `approved`. لا يجوز اعتماد إعادة التوجيه من المتصفح وحدها كدليل دفع.

## متطلبات الإنتاج المتبقية

يلزم قبل الإطلاق التجاري اختبار قاعدة البيانات الفعلية، اختبار عملية التحويل بمبلغ تجريبي معتمد، إعداد بريد أو إشعار لإبلاغ الطالب بعد الاعتماد، وتحديد سياسة الاسترداد وشروط الاستخدام والخصوصية.
